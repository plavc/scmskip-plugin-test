pipeline {
    agent any

    options {
        scmSkipBuild(enabled: true, deleteBuild: true, manuallyTriggered: true)
    }

    stages {
        stage('Build') {
            steps {
                echo "Hello... 23432332"
            }
        }
    }
}
