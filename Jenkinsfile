pipeline {
    agent any

    options {
        scmSkipBuild(enabled: true, deleteBuild: true)
    }

    stages {
        stage('Build') {
            steps {
                echo "Hello... 23432332"
            }
        }
    }
}
