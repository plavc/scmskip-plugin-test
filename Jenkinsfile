pipeline {
    agent any

    options {
        scmSkip(enabled: true, deleteBuild: false, manuallyTriggered: true)
    }

    stages {
        stage('Build') {
            steps {
                echo "Hello... 23432332fff f ff f f"
            }
        }
    }
}
